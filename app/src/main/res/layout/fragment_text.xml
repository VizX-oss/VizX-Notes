<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/note_fragment_holder"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:id="@+id/note_locked_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/m3_spacing_medium"
        android:visibility="gone">

        <ImageView
            android:id="@+id/note_locked_image"
            android:layout_width="@dimen/normal_icon_size"
            android:layout_height="@dimen/normal_icon_size"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="@dimen/m3_spacing_large"
            android:contentDescription="@string/note_content_locked"
            android:src="@drawable/ic_lock_vector" />

        <org.fossify.commons.views.MyTextView
            android:id="@+id/note_locked_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/note_locked_image"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="@dimen/m3_spacing_medium"
            android:paddingStart="@dimen/m3_spacing_large"
            android:paddingTop="@dimen/m3_spacing_small"
            android:paddingEnd="@dimen/m3_spacing_large"
            android:text="@string/note_content_locked" />

        <TextView
            android:id="@+id/note_locked_show"
            style="@style/Widget.Notes.PillButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/note_locked_label"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="@dimen/m3_spacing_medium"
            android:minWidth="48dp"
            android:minHeight="48dp"
            android:text="@string/show_content" />

    </RelativeLayout>

    <ScrollView
        android:id="@+id/notes_scrollview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <RelativeLayout
            android:id="@+id/notes_relative_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </ScrollView>

    <org.fossify.commons.views.MyTextView
        android:id="@+id/notes_counter"
        style="@style/Widget.Notes.PillButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="@dimen/m3_spacing_medium"
        android:textSize="12sp"
        tools:text="123 words" />

</RelativeLayout>
